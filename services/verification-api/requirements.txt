fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
opencv-python-headless>=4.10,<5
python-multipart>=0.0.9
httpx>=0.27.0

# Optional AI V2 inference runtime (torch / torchvision / numpy):
#   install with: pip install -r requirements-ai.txt --index-url https://download.pytorch.org/whl/cu128
# The app starts without these; only the /compare-ai endpoint requires them.